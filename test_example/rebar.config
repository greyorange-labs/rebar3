%% Simple test project to demonstrate parallel CT execution
{erl_opts, [debug_info]}.
{deps, []}.

%% Cover configuration
{cover_enabled, true}.
{cover_opts, [verbose]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]},
        {cover_enabled, true}
    ]}
]}.

%% Enable CT parallel execution
{ct_opts, []}.
